name: lazy_loading_practice
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  json_annotation: ^3.0.1
  http: ^0.12.2
  analyzer: '0.39.14'

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: null
  json_serializable: ^3.4.1

flutter:
  uses-material-design: true
